#!/bin/bash
# Launch Claude Code with prove_it resolving to the local repo's cli.js.
set -e
REPO_ROOT="$(cd "$(dirname "$0")/../../.." && pwd)"
export PATH="$REPO_ROOT/test/bin:$PATH"
exec claude --dangerously-skip-permissions "$@"
