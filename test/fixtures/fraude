#!/bin/bash
# fraude: fake claude for testing text-mode reviewer output
# Usage: fraude -p "prompt"
# Set FRAUDE_RESPONSE env var to control the output text.
# Defaults to "PASS" if not set.
echo "${FRAUDE_RESPONSE:-PASS}"
